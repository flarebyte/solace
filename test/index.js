import assert from 'assert';
import solace from '../lib';

describe('solace', function () {
  it('should have unit test!', function () {
    assert(solace !== null, 'we expected this package author to add actual unit tests.');
  });
});
